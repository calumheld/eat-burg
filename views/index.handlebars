<h1>eat burg</h1>


<img src="https://art.pixilart.com/ef07c29b776befa.png" class="center">
<h2>burgs that are yet to be consumed</h2>

<ul>
    {{#each burgs}}
        {{#unless consumed}}
            {{> burgs/burg-block eaten=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>consumed burgs</h2>

<ul>
    {{#each burgs}}
        {{#if consumed}}
            {{> burgs/burg-block eaten=false}}
        {{/if}}
    {{/each}}
</ul>

<h3>make an offering</h3>
<form class="create-form">
        <div class="form-group">
            <label for="ca">Menu item:</label>
            <input type="text" id="ca" name="name">
        </div>
    <button type="submit">create burg</button>
</form>